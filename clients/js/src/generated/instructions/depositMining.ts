/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@trezoaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@trezoaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  LockupPeriod,
  LockupPeriodArgs,
  getLockupPeriodSerializer,
} from '../types';

// Accounts.
export type DepositMiningInstructionAccounts = {
  /** The address of the reward pool */
  rewardPool: PublicKey | Pda;
  /** The address of the mining account which belongs to the user and stores info about user's rewards */
  mining: PublicKey | Pda;
  /** The address of the Staking program's Registrar, which is PDA and is responsible for signing CPIs */
  depositAuthority: Signer;
  /** The address of Mining Account that might be used as a delegate in delegated staking model */
  delegateMining: PublicKey | Pda;
};

// Data.
export type DepositMiningInstructionData = {
  discriminator: number;
  amount: bigint;
  lockupPeriod: LockupPeriod;
  miningOwner: PublicKey;
  delegate: PublicKey;
};

export type DepositMiningInstructionDataArgs = {
  amount: number | bigint;
  lockupPeriod: LockupPeriodArgs;
  miningOwner: PublicKey;
  delegate: PublicKey;
};

export function getDepositMiningInstructionDataSerializer(): Serializer<
  DepositMiningInstructionDataArgs,
  DepositMiningInstructionData
> {
  return mapSerializer<
    DepositMiningInstructionDataArgs,
    any,
    DepositMiningInstructionData
  >(
    struct<DepositMiningInstructionData>(
      [
        ['discriminator', u8()],
        ['amount', u64()],
        ['lockupPeriod', getLockupPeriodSerializer()],
        ['miningOwner', publicKeySerializer()],
        ['delegate', publicKeySerializer()],
      ],
      { description: 'DepositMiningInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 3 })
  ) as Serializer<
    DepositMiningInstructionDataArgs,
    DepositMiningInstructionData
  >;
}

// Args.
export type DepositMiningInstructionArgs = DepositMiningInstructionDataArgs;

// Instruction.
export function depositMining(
  context: Pick<Context, 'programs'>,
  input: DepositMiningInstructionAccounts & DepositMiningInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tplxRewards',
    'BF5PatmRTQDgEKoXR7iHRbkibEEi83nVM38cUKWzQcTR'
  );

  // Accounts.
  const resolvedAccounts = {
    rewardPool: {
      index: 0,
      isWritable: true as boolean,
      value: input.rewardPool ?? null,
    },
    mining: {
      index: 1,
      isWritable: true as boolean,
      value: input.mining ?? null,
    },
    depositAuthority: {
      index: 2,
      isWritable: false as boolean,
      value: input.depositAuthority ?? null,
    },
    delegateMining: {
      index: 3,
      isWritable: false as boolean,
      value: input.delegateMining ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DepositMiningInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDepositMiningInstructionDataSerializer().serialize(
    resolvedArgs as DepositMiningInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
