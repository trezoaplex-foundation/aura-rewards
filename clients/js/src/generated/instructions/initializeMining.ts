/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/trezoaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@trezoaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@trezoaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeMiningInstructionAccounts = {
  /** The address of the reward pool */
  rewardPool: PublicKey | Pda;
  /** The address of the mining account which belongs to the user and stores info about user's rewards */
  mining: PublicKey | Pda;
  payer?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeMiningInstructionData = {
  discriminator: number;
  miningOwner: PublicKey;
};

export type InitializeMiningInstructionDataArgs = { miningOwner: PublicKey };

export function getInitializeMiningInstructionDataSerializer(): Serializer<
  InitializeMiningInstructionDataArgs,
  InitializeMiningInstructionData
> {
  return mapSerializer<
    InitializeMiningInstructionDataArgs,
    any,
    InitializeMiningInstructionData
  >(
    struct<InitializeMiningInstructionData>(
      [
        ['discriminator', u8()],
        ['miningOwner', publicKeySerializer()],
      ],
      { description: 'InitializeMiningInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 2 })
  ) as Serializer<
    InitializeMiningInstructionDataArgs,
    InitializeMiningInstructionData
  >;
}

// Args.
export type InitializeMiningInstructionArgs =
  InitializeMiningInstructionDataArgs;

// Instruction.
export function initializeMining(
  context: Pick<Context, 'payer' | 'programs'>,
  input: InitializeMiningInstructionAccounts & InitializeMiningInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tplxRewards',
    'BF5PatmRTQDgEKoXR7iHRbkibEEi83nVM38cUKWzQcTR'
  );

  // Accounts.
  const resolvedAccounts = {
    rewardPool: {
      index: 0,
      isWritable: true as boolean,
      value: input.rewardPool ?? null,
    },
    mining: {
      index: 1,
      isWritable: true as boolean,
      value: input.mining ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeMiningInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeMiningInstructionDataSerializer().serialize(
    resolvedArgs as InitializeMiningInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
