/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/trezoaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@trezoaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@trezoaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializePoolInstructionAccounts = {
  /** The address of the reward pool */
  rewardPool: PublicKey | Pda;
  /** The address of the reward mint */
  rewardMint: PublicKey | Pda;
  /** The address of the reward vault */
  vault: PublicKey | Pda;
  payer?: Signer;
  /** Account responsible for charging mining owners */
  depositAuthority: Signer;
  /** The address of the Rent program */
  rent?: PublicKey | Pda;
  /** The address of the Token program where rewards are minted */
  tokenProgram?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializePoolInstructionData = {
  discriminator: number;
  fillAuthority: PublicKey;
  distributeAuthority: PublicKey;
};

export type InitializePoolInstructionDataArgs = {
  fillAuthority: PublicKey;
  distributeAuthority: PublicKey;
};

export function getInitializePoolInstructionDataSerializer(): Serializer<
  InitializePoolInstructionDataArgs,
  InitializePoolInstructionData
> {
  return mapSerializer<
    InitializePoolInstructionDataArgs,
    any,
    InitializePoolInstructionData
  >(
    struct<InitializePoolInstructionData>(
      [
        ['discriminator', u8()],
        ['fillAuthority', publicKeySerializer()],
        ['distributeAuthority', publicKeySerializer()],
      ],
      { description: 'InitializePoolInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 0 })
  ) as Serializer<
    InitializePoolInstructionDataArgs,
    InitializePoolInstructionData
  >;
}

// Args.
export type InitializePoolInstructionArgs = InitializePoolInstructionDataArgs;

// Instruction.
export function initializePool(
  context: Pick<Context, 'payer' | 'programs'>,
  input: InitializePoolInstructionAccounts & InitializePoolInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'tplxRewards',
    'BF5PatmRTQDgEKoXR7iHRbkibEEi83nVM38cUKWzQcTR'
  );

  // Accounts.
  const resolvedAccounts = {
    rewardPool: {
      index: 0,
      isWritable: true as boolean,
      value: input.rewardPool ?? null,
    },
    rewardMint: {
      index: 1,
      isWritable: false as boolean,
      value: input.rewardMint ?? null,
    },
    vault: {
      index: 2,
      isWritable: true as boolean,
      value: input.vault ?? null,
    },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    depositAuthority: {
      index: 4,
      isWritable: false as boolean,
      value: input.depositAuthority ?? null,
    },
    rent: { index: 5, isWritable: false as boolean, value: input.rent ?? null },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    systemProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializePoolInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.rent.value) {
    resolvedAccounts.rent.value = publicKey(
      'SysvarRent111111111111111111111111111111111'
    );
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializePoolInstructionDataSerializer().serialize(
    resolvedArgs as InitializePoolInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
